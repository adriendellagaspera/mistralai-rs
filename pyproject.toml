[build-system]
requires = ["setuptools==75.8.0"]
build-backend = "setuptools.build_meta"

[project]
name = "openapi-to-rust-bindings"
version = "0.1.0"
description = "Normalize openapi-to-rust generated sources into rust-sdk-compiler Bindings"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT OR Apache-2.0"}
dependencies = [
  "rust-sdk-compiler @ git+https://github.com/adriendellagaspera/mistralai-rs.git@5bda3f6b704edf5b6a9d8788010c4117321ceb5e",
  "tree-sitter==0.25.2",
  "tree-sitter-rust==0.24.0",
]

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["openapi_to_rust_bindings"]
