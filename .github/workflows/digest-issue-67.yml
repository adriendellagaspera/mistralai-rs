name: Digest issue 67 patch

on:
  push:
    branches: [issue-67-openapi-overlays]

jobs:
  digest:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: issue-67-openapi-overlays
      - run: sha256sum tooling/pipeline/openapi-to-rust.patch
